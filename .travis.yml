language: python

python:
  - "3.6"
  - "3.7"

install:
  - pip install -r requirements.txt

script:
  - coverage run test_LSTM.py -v

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email: false
